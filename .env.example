# Server Configuration
NODE_ENV=development
PORT=5000
HOST=0.0.0.0

# Database Connection (REQUIRED)
# Use PostgreSQL - either local or Neon
DATABASE_URL=postgresql://postgres:postgres@localhost:5432/ai_nexus

# Session Secret (REQUIRED)
# Generate one with: openssl rand -hex 32
# On Windows, use: $([System.Convert]::ToBase64String([System.Security.Cryptography.RNGCryptoServiceProvider]::new().GetBytes(32)))
SESSIONS_SECRET=your_secret_key_here_change_in_production

# OpenRouter API Key (REQUIRED for AI/Bot features)
# Get from: https://openrouter.ai/keys
OPENROUTER_API_KEY=your_openrouter_api_key_here

# Frontend API URL (for production deployments)
VITE_API_URL=http://localhost:5000

# Optional: OpenRouter Model Selection
OPENROUTER_MODEL=meta-llama/llama-2-70b-chat
